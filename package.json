{
  "name": "anytv-node-quota",
  "version": "0.0.1",
  "description": "express middleware for handling api quota",
  "main": "lib/index.js",
  "scripts": {
    "compile": "babel -d lib/ src/",
    "prepublish": "npm run compile",
    "test": "mocha --compilers js:babel-core/register test/*"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "assert": "^1.3.0",
    "babel": "^6.0.15",
    "babel-cli": "^6.1.2",
    "babel-preset-es2015": "^6.1.4",
    "chai": "^3.4.1",
    "lodash": "^3.10.1",
    "mocha": "^2.3.3"
  }
}
